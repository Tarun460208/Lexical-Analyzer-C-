# Lexical-Analyzer-C-
Implemented a lexical analyzer to identify tokens from source code.  Technologies Used C, String Processing, Parsing . Differentiated keywords and identifiers using structured logic, achieving ~95% accuracy. Reduced token misclassification by ~80% using keyword lookup validation. Improved parsing stability for complex input patterns.
